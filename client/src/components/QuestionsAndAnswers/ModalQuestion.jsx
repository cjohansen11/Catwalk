import React, {useState} from 'react';

const ModalQuestion = ({show, showModal}) => {



  return (
    <>
      <button onClick={e => {
        this.showModal(e);
      }}
      > show Modals </button>
    </>
  );



};

export default ModalQuestion;